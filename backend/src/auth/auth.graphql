type Mutation {
  login(username: String!, password: String!): Token!
  singup(username: String!, password: String!): UserDto!
}

type Token {
  id: ID!
  access_token: String!
}

type UserDto {
  username: String!
  password: String!
}